# Copy to .env (or set on your hosting platform) and point at the Django API.
# Local dev falls back to http://127.0.0.1:8000 automatically, so this is
# mainly needed for production builds (e.g. on Vercel).
VITE_API_URL=https://your-backend.onrender.com
